# Endpoint opcional para o formulario de contato (POST JSON: { name, email, message }).
# Se ficar vazio, o formulario cai automaticamente no fallback via mailto:.
NEXT_PUBLIC_CONTACT_ENDPOINT=

# URL publica do site (usada em metadata, sitemap e robots).
NEXT_PUBLIC_SITE_URL=https://guilherme.dev
